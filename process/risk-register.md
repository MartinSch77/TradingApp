# Risk Register

Real, running register (`work-products/risk-register.md`'s spec). Scored per
`strategies/risk-management-strategy.md` (likelihood × impact, 1–25).

| ID | Source | Description | Likelihood | Impact | Score | Category | Owner | Status | Last reviewed | Treatment |
|---|---|---|---|---|---|---|---|---|---|---|
| RISK-001 | Engineering investigation (`tools/squish_run.sh`'s own in-script record) | Coco GUI coverage still produces no `.csexe`. UPDATED 2026-08-14: the 2026-08-13 hypothesis ("Squish kills the AUT instead of letting it `exit()`") was tested directly and DISPROVED. Every `squish/suite_gui` test now ends by closing the main window (`names.closeAppGracefully`), driving a normal `quitOnLastWindowClosed` -> `QApplication::exec()` return -> `main()` return -> C++ runtime `exit()`. Live `pgrep` polling during a run confirmed the AUT process exits ON ITS OWN roughly 8 s after that call, well before squishrunner's own test-case teardown ~20 s later — a provably voluntary, non-killed exit. Still zero `.csexe` anywhere. The SAME instrumented binary run directly with no Squish involved (`TRADINGAPP_SHOT`) reliably writes `.csexe` on a normal quit, so the atexit writer itself works; the difference is specifically being launched by squishserver. Coco's own manual (`__coveragescanner_register_squish()`) states that an Coco-instrumented AUT started by Squish hands control of report-writing OVER to Squish's own runtime hook automatically — this Squish for Qt 9.2.2 installation's documentation names no test-script function, `--config` action or environment variable that hands that control back. | 2 | 2 | 4 | Technical | Unassigned | open | 2026-08-14 | Escalate to Squish/froglogic support: ask specifically whether Squish for Qt 9.2.2 (vs. a bundled "Squish Coco" product) implements the `__coveragescanner_register_squish()` write-back and what triggers it — this needs vendor confirmation the tools installed here cannot provide. The graceful-close teardown (`tools/squish_run.sh`, `squish/suite_gui/shared/scripts/names.py`) is kept as an independent GUI-suite hygiene improvement regardless of the coverage outcome |
| RISK-002 | Requirements review (this session) | REQ-F-034 and REQ-F-035 each bundle roughly ten independent obligations under one requirement id, weakening the meaning of "traced to a test" for either (~15 and ~23 existing test references respectively cite the bundled id rather than a specific obligation). | 3 | 3 | 9 | Process | Requirements Engineer | mitigating | 2026-08-13 | Scoped atomization plan recorded in `process/requirements-schema-upgrade.md`; tracked via a `process-improvement` GitHub issue rather than split without individual test re-verification |
| RISK-003 | QA self-audit (`tools/qa_report.py`, first run) | `CHANGELOG.md` (application-level) does not exist at the repository root, though several work-product specs (`work-products/project-plan.md`, `configuration-baseline.md`) assume its presence as planning/release evidence. | 2 | 2 | 4 | Process | Project Manager | closed | 2026-08-13 | Closed by PR #19: root `CHANGELOG.md` added, `tools/qa_report.py` re-run confirms CONFIRMED |
| RISK-004 | v1.1.0 release pipeline, `tools/sanitize.sh tsan` (GitHub Issue #20) | Intermittent ThreadSanitizer data race in `JsonHttp::handleReply`'s connection-cleanup path (worker-thread `deleteOrphaned` racing the main thread's connection construction). RESOLVED by PR #22 (`d01a230`, 2026-08-14): `Qt::SingleShotConnection` on the `finished()` connection plus documented `tools/tsan.supp` entries. A 2026-08-16 v1.1.1 clean-build-release run (`TestEtoroClient::TS_CLI_008`) appeared to reproduce the SAME race, but was traced to this branch's own local `main` being 7 commits stale — it had never pulled PR #22's fix in, so that run was unknowingly exercising PRE-fix `JsonHttp.cpp`. Not a new/independent sighting of a persisting bug; corrected here rather than left as a false "still open, second occurrence" entry. Re-verified AFTER merging the real fix in: `tools/sanitize.sh tsan` clean, 3 additional isolated re-runs of `tst_etoroclient` clean. | 1 | 3 | 3 | Technical | Unassigned | closed | 2026-08-16 | Closed by PR #22; this register entry itself was corrected 2026-08-16 after a stale local branch made the already-fixed bug look like a live recurrence — a reminder to `git fetch`/reconcile with `origin/main` before trusting a re-run's evidence, not just before publishing |

No Critical (20–25) or currently-untreated High (13–19) risk is open as of
this register's creation.
